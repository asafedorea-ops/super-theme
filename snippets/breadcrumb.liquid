<!-- start breadcrumb.liquid (SNIPPET) -->
{%- unless template == 'index' or template == 'cart' -%}
  <div class="breadcrumbs" aria-label="breadcrumbs">
    <a id="breadcrumbs__homepage" class="breadcrumbs__item breadcrumbs__link" href="/" title="Home">Home page</a>

    {%- if template contains 'collection' and collection.handle -%}
      <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>

      {%- if current_tags -%}
        <a id="breadcrumbs__collection" class="breadcrumbs__item breadcrumbs__link" href="{{ collection.url }}" title="{{ collection.title | strip_html | escape }}">{{ collection.title }}</a>
        <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>
        <span class="breadcrumbs__item">{{ current_tags | join: " + " | replace: "-" , " " }}</span>
      {%- else -%}
        <span class="breadcrumbs__item">{{ collection.title }}</span>
      {%- endif -%}
    {%- elsif template == 'blog' -%}
      <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>

      {%- if current_tags -%}
        <a id="breadcrumbs__blog" class="breadcrumbs__item breadcrumbs__link" href="{{ blog.url }}" title="{{ blog.title | strip_html | escape }}">{{ blog.title }}</a>
        <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>
        <span class="breadcrumbs__item">{{ current_tags | join: " + " }}</span>
      {%- else -%}
        <span class="breadcrumbs__item">{{ blog.title }}</span>
      {%- endif -%}
    {%- elsif template == 'article' -%}
      <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>

      <a id="breadcrumbs__article" class="breadcrumbs__item breadcrumbs__link" href="{{ blog.url }}" title="{{ blog.title | strip_html | escape }}">{{ blog.title }}</a>
      <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>
      <span class="breadcrumbs__item">{{ article.title }}</span>
    {%- elsif template == 'product' -%}
      {%- if collection.url -%}
        <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>
        <a id="breadcrumbs__product-url" class="breadcrumbs__item breadcrumbs__link" href="{{ collection.url }}" title="{{ collection.title | strip_html | escape }}">{{ collection.title }}</a>
      {%- endif -%}
        <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>
        <span class="breadcrumbs__item">{{ page_title }}</span>
      </span>
    {%- else -%}
    <span class="breadcrumbs__separator" aria-hidden="true">&rsaquo;</span>
    <span class="breadcrumbs__item">{{ page_title }}</span>
    {%- endif -%}
  </div>
{%- endunless -%}